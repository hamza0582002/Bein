{
  "name": "maktabate-reader",
  "version": "1.0.0",
  "private": true,
  "description": "Maktabate — قارئ كتب احترافي مع مكتبة ثلاثية الأبعاد تحاكي المكتبة الواقعية",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "icons": "python3 tools/make-icons.py && python3 tools/make-android-assets.py",
    "android:sync": "npm run build && cap sync android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "jszip": "^3.10.1",
    "pdfjs-dist": "^4.10.38"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
